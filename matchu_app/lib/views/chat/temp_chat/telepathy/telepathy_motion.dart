import 'package:flutter/animation.dart';

/// ================== DURATION ==================
const motionFast = Duration(milliseconds: 120);
const motionBase = Duration(milliseconds: 240);
const motionSlow = Duration(milliseconds: 420);

/// ================== CURVE ==================
const curveEnter = Curves.easeOutCubic;
const curveExit = Curves.easeInCubic;
const curveFeedback = Curves.easeOutBack;

/// ================== FEEDBACK ==================
const shakeAmplitude = 8.0;
