import 'package:flutter/material.dart';

class RandomChatView extends StatelessWidget {
  const RandomChatView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}