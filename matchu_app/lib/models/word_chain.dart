enum WordChainStatus {
  idle,
  inviting,
  countdown,
  playing,
  reward,
  finished,
}

enum WordChainQuestionCard {
  memory,
  opinion,
  hobby,
}

enum WordChainRewardPhase {
  idle,
  asking,
  answering,
  reviewing,
  done,
}
